// Re-export shim → citrate-studio-ui-kit (see theme.slint).
export {
    Palettes, RiskDot, RiskBadge, DataChip, RoleGlyph, SurfBadge, SevDot,
    AnchorMark, Keyframe, Hairline, Btn, IconButton, Card,
} from "@citrate-ui-kit";
