// Re-export shim — the real design system lives in the `citrate-studio-ui-kit`
// crate (../ui-kit/), registered as the `@citrate-ui-kit` Slint library. This
// shim keeps every `from "theme.slint"` import in the components working unchanged.
export { Theme, Settings } from "@citrate-ui-kit";
